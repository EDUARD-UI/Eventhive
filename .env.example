# URL del backend desplegado (opcional).
# Si se deja vacío o no existe el archivo .env, la app usará el backend desplegado de EventHive
# VITE_API_URL=https://eventhive-backend-mr93.onrender.com/api
